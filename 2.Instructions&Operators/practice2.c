#include <stdio.h>

int main()
{
    int r;
    printf("Enter the no is ");
    scanf("%d", &r);
    printf("The remainder of no is %d\n", r%97);

    //question 4//
    int x=2,y=3,z=3,k=1;
    printf("calulate value of operation is %d\n",3*x/y-z+k);
    return 0;
}